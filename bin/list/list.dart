import 'dart:collection';

void main() {
  List<String> names = ['aro', 'argon', 'argen'];
  print('the value $names');
  print('prin nname ${names[0]}');
  print('prin nname ${names[1]}');
  print('prin nname ${names[2]}');

  int length = names.length;
  print('we can now lengh $length');
}

// / If you want to store multiple values without creating multiple variables, you can use a list.